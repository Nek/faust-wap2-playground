{
  "name": "faust-wap2",
  "version": "0.1.0",
  "watch": {
    "compile": "dsp/melody.dsp"
  },
  "scripts": {
    "start": "vite --open & npm-watch",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "compile": "faust2wasm dsp/melody.dsp -worklet && rm melody.js melody-processor.js && mv melody.wasm assets"
  },
  "browser": {
    "process": false,
    "setTimeout": false,
    "util": false
  },
  "devDependencies": {
    "npm-watch": "^0.11.0",
    "prettier": "^2.5.1",
    "typescript": "^4.5.5",
    "vite": "^2.8.2"
  }
}
